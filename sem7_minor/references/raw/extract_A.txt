C01 | Wang et al., 2020 | Binary real/fake CNN classifier with careful pre-/post-processing and Blur+JPEG data augmentation | ResNet-50 (ImageNet-pretrained) | ForenSynths (720K train images ProGAN real/fake; 11-generator test sets; LSUN reals) | ProGAN (train); tested on StyleGAN, StyleGAN2, BigGAN, CycleGAN, StarGAN, GauGAN, DeepFakes, CRN, IMLE, SAN, SITD | mAP 90.8 avg over 11 unseen-generator test sets with Blur+JPEG(0.5) aug (90.1 without aug) | yes - trained ProGAN, tested 11 others | Performs at chance on traditional (Photoshop-style) manipulations; "for now" caveat that future generators may evade it
C02 | Chai et al., 2020 | Patch-based classification with limited receptive fields; heatmaps of which patches are detectable | Truncated Xception and ResNet patch classifiers (Xception blocks best) | CelebA-HQ (24,183 train), FFHQ (60,000 train), FaceForensics++ | ProGAN, StyleGAN, StyleGAN2, Glow, GMM face generator; FF++ manipulations (Deepfakes, Face2Face, FaceSwap, NeuralTextures) | AP 99.99 training on ProGAN, testing on StyleGAN (patch classifier); AP ~100 across unseen resolutions | yes - ProGAN-trained, tested StyleGAN/Glow/GMM | "No method is completely bulletproof" - better generators, out-of-distribution images, or adversarial attacks can defeat it
C03 | Gragnaniello et al., 2021 | Critical benchmark of SOTA GAN-image detectors under realistic conditions (compression, resizing, unseen generators) | Seven detectors: Xception, SRNet, Spec (ResNet34 on spectrum), M-Gb, Co-Net, Wang2020 ResNet50, PatchForensics + proposed no-down variant | 362K LSUN reals + 362K ProGAN fakes (train); test reals from ImageNet, COCO, RAISE (~39K synthetic test) | ProGAN (train); tested StyleGAN, StyleGAN2, BigGAN, CycleGAN, StarGAN, RelGAN, GauGAN | AUC above 0.9 for several methods on uncompressed low-res unseen GANs; severe drops after compression/resizing ("2x downsampling has catastrophic effects"); best no-down variant above 90% accuracy | yes - ProGAN-trained, 7 unseen GANs | Authors state "we are still very far from having reliable tools for GAN image detection"; robustness experiments "very limited and preliminary"
C04 | Yu et al., 2019 | Learned GAN fingerprints for real/fake classification and closed-set source attribution | Deep CNN attribution classifier (fingerprint = final 1x1x512 feature/weight tensors); plus AutoEncoder fingerprint-visualization variant | CelebA and LSUN bedroom (20,000 reals each; 100K train / 10K test images per source) | ProGAN, SNGAN, CramerGAN, MMDGAN (all outputs fixed 128x128x3) | Accuracy 99.43% (CelebA) / 98.58% (LSUN) on 5-class {real + 4 GANs} attribution, vs PRNU baseline 86.61/67.84 | no - closed-set attribution, known GANs | Closed-set: needs source-labelled samples from every candidate GAN; evaluated only at 128x128 output size
C05 | Marra et al., 2018 | PRNU-style hand-crafted GAN fingerprint: average of denoising residuals, correlation-based source identification | Denoising-filter residual averaging (no learned classifier) | 1,000 RGB 256x256 images per source; RAISE camera images (Nikon-D90, Nikon-D7000); CycleGAN/ProGAN/StarGAN category sets (apple2orange, horse2zebra, bedroom, etc.) | CycleGAN (9 configs), ProGAN (6 configs), StarGAN (5 configs) | Source-attribution accuracy 90.3% uncompressed, 90.1% after JPEG QF=95 [VERIFY exact table values] | no - source ID among known GANs | Weaker on StarGAN male/smiling networks and on real cameras (low-energy PRNU patterns)
C06 | Zhang et al., 2019 | Frequency-spectrum classifier targeting GAN upsampling artifacts; AutoGAN simulator generates artifacts without needing real fakes | ResNet-34 (ImageNet-pretrained) on log frequency spectrum (phase discarded) | CycleGAN 14-category image sets (horse, zebra, Yosemite summer/winter, etc.) + 4,000 MSCOCO images for AutoGAN training | CycleGAN (primary), StarGAN, GauGAN tested | Accuracy 97.2 avg (leave-one-out) for AutoGAN-trained spectrum classifier that never saw real fake images; 99.8 for CycleGAN-trained spectrum classifier | yes - detects unseen GANs via simulated artifacts | All classifiers fail on GauGAN (different nearest-neighbour upsampler); sensitive to up-sampler mismatch and training-category choice
C07 | Frank et al., 2020 | DCT frequency-domain analysis of grid-like upsampling artifacts with simple classifiers | Ridge regression and shallow 4-conv-layer CNN (~170K params) on DCT spectra (vs kNN, Eigenfaces, PRNU baselines) | FFHQ, CelebA, LSUN bedrooms | StyleGAN, BigGAN, ProGAN, SN-DCGAN, CramerGAN, MMDGAN | Ridge-Regression-DCT 100.00% accuracy real-vs-fake on FFHQ (vs 75.78% on pixels); CNN-DCT source ID 99.64% LSUN / 99.07% CelebA | [VERIFY] - abstract claims cross-architecture artifact consistency, no cross-train/test number fetched | Authors note classifier can still be evaded by specifically crafted adversarial perturbations
C08 | Durall et al., 2020 | Azimuthal integration of DFT power spectrum into 1D spectral feature + classical classifiers; also proposes spectral regularization for generators | SVM, logistic regression, K-means (unsupervised) on spectral feature | Faces-HQ (40K, self-built), CelebA (202,599), FaceForensics++ | DCGAN, DRAGAN, LSGAN, WGAN-GP (CelebA); FaceForensics++ deepfakes; up-convolution GANs generally | Accuracy 100% on Faces-HQ (SVM, 1000 samples) and 100% on CelebA (2000 samples); 85% on FaceForensics++ single frames (90% full video) | no - per-dataset supervised/unsupervised tests only | Detection assumes up-convolution generators; other generation techniques may not show these spectral signatures
