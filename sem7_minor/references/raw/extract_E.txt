C34 | Cozzolino et al., 2024 | Zero-shot entropy-based forensics (coding cost of real-image model, trained on real images only) | Lossless multi-resolution neural image encoder (ZED) | Open Images (~338k real images) for encoder training; 19-generator synthetic test set | 19 generators incl. GauGAN, BigGAN, StyleGAN2, GigaGAN, ADM, GLIDE, SD/SDXL, DiT, DALL-E 1/2/3, Midjourney v5, Firefly | ~90.0% avg AUC (|Delta01| statistic) across 19 generators; >3% avg accuracy gain over SoTA | yes - zero-shot, 19 unseen generators | Unreliable on heavily compressed/resized images; detects fully generated images only, not local manipulations
C35 | Park et al., 2025 | Supervised detector trained on massively diverse multi-generator dataset | ViT-S (CLIP-pretrained) classifier; ConvNeXt-S also tested | Community Forensics (2.7M images, 4,803 generators); eval on Wang, Ojha, Synthbuster, GenImage + own 21-model eval set | 4,803 generators: latent/pixel diffusion, GANs, commercial models | 0.987 mAP and 89.2% accuracy on out-of-distribution eval set (21 unseen models) | yes - 21 unseen models, 5 generator families | Dataset skewed toward diffusion/Stable-Diffusion models; error rates still too high for critical applications
C36 | Yan et al., 2024 | Hybrid low-level (frequency/noise) + high-level (semantic) mixture-of-experts detection; proposes in-the-wild Chameleon benchmark | AIDE: DCT scoring module + SRM noise filters + OpenCLIP semantic encoder | AIGCDetectBenchmark, GenImage, Chameleon (proposed) | ProGAN, StyleGAN/2, BigGAN, CycleGAN, StarGAN, GauGAN, ADM, Glide, SD v1.4/1.5, VQDM, Wukong, Midjourney, DALLE-2/3 | 92.77% mean acc on AIGCDetectBenchmark, 86.88% on GenImage; drops to 65.77% on Chameleon | yes - 16+ generators across benchmarks | All tested detectors near-fail on realistic Chameleon images; problem "far from being solved"
C37 | Ojha et al., 2023 | Real-vs-fake classification without training the backbone: nearest-neighbor / linear probing in frozen vision-language feature space | CLIP ViT-L/14 visual encoder (last-layer features) + NN or linear probe | ProGAN real/fake training set (720k images, Wang et al.); ForenSynths + diffusion/AR test sets | ProGAN, StyleGAN, BigGAN, CycleGAN, StarGAN, GauGAN, CRN, IMLE, SAN, SITD, DeepFakes, Guided, LDM, Glide, DALL-E | +15.07 mAP / +25.90% acc over SoTA on unseen diffusion and autoregressive models (~82-84% avg acc on unseen models) | yes - GAN-trained, tested on diffusion/AR | Why frozen CLIP feature space separates real from fake "remains an open question"
C38 | Cozzolino et al., 2023 | Few-shot CLIP-feature forensics: linear SVM on frozen CLIP features from a handful of reference images | CLIP ViT-L/14 (next-to-last-layer features) + linear SVM | Few paired real/fake reference images (10 to 10k) from a single generator; 18-generator test suite | 18 generators: ProGAN, StyleGAN2/3/T, GigaGAN, Score-SDE, ADM, GLIDE, eDiff-I, LDM, SD variants, DiT, DeepFloyd-IF, DALL-E 2/3, Midjourney v5, Firefly | ~90.0% avg AUC across 18 generators (10k variant, post-processed images); +6% AUC OOD and +13% on impaired data vs SoTA | yes - 18 generators, 3 families | Interpretability lacking: which forensic features CLIP exploits is not yet understood
C39 | Koutlis et al., 2024 | Synthetic-image detection from intermediate CLIP encoder-block representations with learnable importance weighting | RINE: frozen CLIP ViT-L/14 intermediate blocks + lightweight trainable projection/weighting network | ProGAN training data (1/2/4-class settings); 20 test datasets | ProGAN, StyleGAN/2, BigGAN, CycleGAN, StarGAN, GauGAN, DeepFake, SITD, SAN, CRN, IMLE, Guided, LDM (3), Glide (3), DALL-E | 91.5% avg accuracy and 98.8 AP (4-class training) across 20 test sets; +10.6% ACC over SoTA | yes - 20 test sets, GANs + diffusion | Combined perturbations (blur + crop + compression + noise) considerably reduce performance
C40 | Liu et al., 2023 | Forgery-aware adaptive transfer of CLIP: adapter over image + frequency domains with language-guided alignment | FatFormer (CLIP-ViT + forgery-aware adapter + text-prompt alignment) | ProGAN 4-class training set; unseen GAN and diffusion test sets | GANs and diffusion models (unseen at test time) | 98% accuracy on unseen GANs, 95% on unseen diffusion models | yes - unseen GANs and diffusion models | Trained on ProGAN data only; no self-stated limitation in fetched text [VERIFY]
C41 | Tan et al., 2024 | Category-common prompt injected into CLIP text encoder to embed detection semantics into image features | C2P-CLIP (CLIP ViT-L/14 with category common prompt) | UniversalFakeDetect (ProGAN train, 19 test subsets) and GenImage (SDv1.4 train) | 20 generation models (UniversalFakeDetect subsets + GenImage diffusion models) | 93.79% accuracy / 98.66% mAP on UniversalFakeDetect 19 test subsets; 95.8% acc on GenImage | yes - 20 generation models | Word-frequency prompt analysis lacks full-caption analysis, giving incomplete information
