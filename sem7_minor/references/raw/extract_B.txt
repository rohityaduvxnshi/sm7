C09 | Liu et al., 2020 | Global texture (Gram) statistics for fake face detection | Gram-Net (CNN leveraging global texture representations) | CelebA-HQ, FFHQ, CelebA (real); GAN face sets; ImageNet/BigGAN (natural) | StyleGAN, PGGAN, DCGAN, DRAGAN, StarGAN, BigGAN | ~77.7% acc cross-GAN (train StyleGAN, test DCGAN/DRAGAN/StarGAN); >98% in-domain | Yes - unseen GAN models evaluated | Authors state analysis covers only texture contrast; real/fake differences go beyond it
C10 | Nataraj et al., 2019 | Pixel-domain co-occurrence matrices fed to deep CNN | Deep CNN on 3-channel co-occurrence matrices | Two GAN datasets, 56,000+ images (unpaired translation; facial attributes) | CycleGAN, StarGAN | >99% classification accuracy on both datasets | Yes - trained on one GAN, tested on the other | Only two GAN families evaluated; no explicit limitation stated in abstract
C11 | Cozzolino et al., 2021 | Contrastive learning with limited sub-sampling architecture | ResNet50 (ImageNet-pretrained, no first-layer subsampling) + 2 projection layers | LSUN (362K real) + 362K ProGAN images (train); ImageNet, COCO, RAISE (test) | ProGAN (train); StyleGAN, StyleGAN2, BigGAN, CycleGAN, StarGAN, RelGAN, GauGAN (unseen test) | 95.6% acc / 0.997 AUC avg on unseen GAN architectures | Yes - seven unseen GAN architectures | Robustness to adversarial attacks left to future work
C12 | Jeong et al., 2022 | Frequency-level perturbation maps (FrePGAN) to suppress frequency artifacts | Perturbation generator + ResNet-50 deepfake classifier | ProGAN-generated train set; test images from FFHQ, LSUN, ImageNet, CelebA, COCO, Deepfake sets | ProGAN, StyleGAN, StyleGAN2, BigGAN, CycleGAN, StarGAN, GauGAN | 79.4% avg acc / 87.2% avg precision cross-model (4 training classes) | Yes - known and unseen GAN models | Tradeoff: frequency artifacts aid known-GAN detection but reduce cross-GAN generalization
C13 | Mandelli et al., 2022 | Ensemble of orthogonally trained CNNs favoring real-image scores | Ensemble of five EfficientNet-B4 CNNs | FFHQ, Metfaces, AFHQ2 (~116K real) + ~200K synthetic (train); StyleGAN3 test pairs | StyleGAN2, StarGAN-v2, Taming Transformers, FaceVid2Vid, score-based models; StyleGAN3 (unseen) | 0.9995 AUC on global test set incl. unseen StyleGAN3 | Yes - StyleGAN3 excluded from training | Orthogonality among CNNs only empirically verified; no theoretical grounding
C14 | Ju et al., 2022 | Fusion of global spatial and local patch features via attention | Two-branch ResNet-50 (ImageNet-pretrained) + patch selection module + multi-head attention | Self-built diverse test set: 128,424 images from 19 generation models | 19 models: ProGAN, StyleGAN/2/3, BigGAN, WGAN, VAEGAN, GauGAN, StarGAN, CycleGAN, Pix2Pix, CRN, IMLE, SITD, SAN, FF++, Celeb-DF, Transformers, BGM | 91.73% mean AP on unseen models (96.91% global AP mixed) | Yes - unseen-model evaluation is central | Resizing high-resolution images makes artifacts harder to detect (Low-level Vision/DeepFakes families)
C15 | Tan et al., 2023 | Learning on Gradients (LGrad): gradients from pretrained CNN as generalized artifact representation | Pretrained CNN transformation model converting images to gradients + classifier | CNNDetection benchmark data (per code repo) [VERIFY exact splits] | Multiple GANs incl. StyleGAN (per repo); full list [VERIFY] | New state-of-the-art, +11.4% gain, cross-model/cross-data setting (absolute value [VERIFY]) | Yes - cross-model and cross-data aim, unseen domains | Turns data-dependent problem into a transformation-model-dependent one (authors' own framing)
C16 | Tan et al., 2023 | Neighboring Pixel Relationships (NPR) capturing up-sampling artifacts | Lightweight CNN (conv layers + ResNet blocks, 1.44M params) on NPR | ForenSynths ProGAN 4-class train (car, cat, chair, horse; 18K fake + 18K real per class) | 28 generative models: 16 GANs, 1 deepfake method, 11 diffusion models | 92.2% mean acc across 28 generators (+11.6% over prior SOTA) | Yes - 28 unseen GAN and diffusion models | No explicit limitation stated; trained solely on ProGAN 4-class data
